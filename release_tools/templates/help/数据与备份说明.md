# 数据与备份说明

所有个人内容都在顶层 `UserData/`：

- `canvases/`：画布；
- `assets/`：导入素材；
- `outputs/`：生成结果；
- `config/`：API 与本地配置；
- `backups/`：升级相关备份；
- `diagnostics/`：用户主动导出的脱敏诊断包。

备份时复制整个 `UserData/`。不要只复制 `Application/`，也不要把自己的 `UserData/` 发给朋友。

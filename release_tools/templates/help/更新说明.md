# 更新说明

日常更新只替换 `Application/current`，不会覆盖 `UserData`。

收到升级 ZIP 后：

1. 解压整个 `CIEL Canvas Update` 文件夹；
2. 双击或右键打开 `安装 CIEL 更新.app`；
3. 按提示选择现有的 `CIEL Canvas` 根目录；
4. 等待校验、备份、切换和健康检查完成。

升级器完全离线工作，不会联网下载程序。

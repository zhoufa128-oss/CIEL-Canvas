# CIEL Canvas 使用说明

1. 将整个 `CIEL Canvas` 文件夹解压到可写位置。
2. 在 Finder 中右键 `CIEL Canvas.app`，选择“打开”。
3. 首次启动会创建独立的 `UserData/`，并显示“欢迎使用 CIEL Canvas”画布。
4. 以后可以直接双击 App；服务会继续在后台运行，重复双击不会启动第二份服务。

本包为 macOS arm64 构建，不代表兼容 Intel Mac，也未经过 Apple 官方签名或公证。
